import React from 'react'
import NavBar from '../Navbar'
import {HeroContainer} from '../Hero/HeroElements'

const Hero = () => {
  return (
    <HeroContainer>
      <NavBar />
    </HeroContainer>
  )
}

export default Hero;